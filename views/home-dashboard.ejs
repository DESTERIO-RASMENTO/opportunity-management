    <%- include('includes/header') %>
    <!-- header ends here -->

    <div class="container py-md-5 container--narrow">
        <% if(account.length) {%>
            <div class="list-group">
                <h2 class="text-center mb-4">Your Accounts</h2>

                <% account.forEach(function(account){ %>
                <a href="/create/<%= account._id %>" class="list-group-item list-group-item-action">
                    <img class="avatar-tiny" src="<%= account.author.avatar %>">
                    <strong><%= account.title %></strong> Account
                </a>
                <% }) %>
            </div>
        <%}else{%>
            <div class="text-center">
                <h2>Welcome <strong><%= user.username %></strong>, You have not created an account for managing you
                    Opportunities.</h2>
                <p class="lead text-muted">Lorem ipsum dolor, sit amet consectetur adipisicing elit. Consequuntur in
                    reprehenderit aliquam deserunt quod non, maiores porro, repudiandae, quas deleniti repellendus
                    dolorum
                    fuga ullam voluptate? Earum quos quidem similique porro!</p>
            </div>
        <%}%>
    </div>

    <!-- footer begins -->
    <%- include('includes/footer') %> 